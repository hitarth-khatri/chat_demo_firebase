import 'package:flutter/material.dart';

class AppIcons {
  static const logoutIcon = Icon(Icons.logout);
  static const messageIcon = Icon(
    Icons.message,
    color: Colors.indigo,
  );
  static const sendIcon = Icon(Icons.send);
}
